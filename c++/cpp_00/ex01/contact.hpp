#ifndef CONTACT_HPP
# define CONTACT_HPP

# include "utiles.hpp"

class Contact {

public :
	Contact(void);
	// first(void);
	// last(char);
	// nick(char);
	// phone_num(char);
	// secret(char);

	~Contact(void);
	
};

#endif